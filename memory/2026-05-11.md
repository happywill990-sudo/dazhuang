# 2026-05-11

## 三条模型配置排查
- 三条默认模型：MiniMax-M2.5，备用 DeepSeek-Reasoner
- 历史曾用 gpt-5.4

## Opus 4.7 SSE 损坏问题
- 三条配置了两个 Opus 4.7 路由：compshare/claude-opus-4-7（api.modelverse.cn）和 mt/claude-opus-4-7（api.9cubic.cn）
- subagent 裸 ID `claude-opus-4-7` 在 merge 模式下被解析到 mt 版本
- **根因：mt 代理（api.9cubic.cn）** 返回了损坏的 SSE 流，`message_stop` 事件中混入了 `message_start` 的 payload，是服务器端帧拼接/边界处理错误
- compshare 路由正常，非 Anthropic 官方 API 问题
- 建议稳定使用 Opus 4.7 时指定 `compshare/claude-opus-4-7` 避免 merge 误解析
- 另：飞书 contact:contact 权限缺失（99991672），如需要三条查通讯录需补权限

## Gateway 命令通道超时
- 14:20 左右 "main" lane 卡死 330s
- 导致后续 subagent announcement 重试 4 次全部失败，触发 incomplete turn 保护逻辑
- 连锁根因：Opus 4.7 SSE 异常 → lane 超时 → announcement 失败

## 我与三条的关系
- 三条是同一台机器上的第一个 OpenClaw 实例
- 差异：三条默认模型较低（MiniMax M2.5），但可以通过切换模型使用高级模型
- 我是第二个实例（大壮），定位编码/运维
- 三条走飞书通道，我也已接入飞书

## 模型切换操作
- 应 Will 要求，将三条的默认模型从 `minimax/MiniMax-M2.5` 切换为 `mt/claude-opus-4-7`
- 三条 gateway 端口为 18789，通过修改 `~/.openclaw/openclaw.json` 中 `model.primary` 完成
- 同时将 `mt/claude-opus-4-7` 的 `models` 配置更新为 `{"alias": "Opus4.7(mt)"}`
- 执行 `openclaw gateway restart -p 18789` 重启生效，验证状态：三条 running
- 切换时间：2026-05-11 约 15:03 CST
