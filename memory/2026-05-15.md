# 2026-05-15 Memory

## 内存持久化 (Pre-compaction flush)

### 活跃会话
- 通过飞书与 Will 对话中
- 当前请求：打开微信联系人"银杏村"的聊天对话框并截图
- 已通过 Playwright 脚本启动微信 Web 版（http://localhost:3000/?default_app=wechat）
- 已登录微信网页版并截取当前界面

### 关键状态
- WeChat Web 版通过自建 webapp（/home/w/OCCoding/wechat-webapp）运行在 localhost:3000
- 用户发的是微信聊天界面截图的请求
- 微信网页版已打开，需要导航到"银杏村"的聊天

### 等待后续操作
- 尝试通过 Playwright 在微信网页版中搜索联系人"银杏村"
- 如无法自动操作，需人工指导

## Turn 2 — 用户问"能打开微信吗？"

### 用户消息
"能打开微信吗？" — 用户想让我打开微信，并继续之前"打开银杏村对话框截图"的请求。

### 当前状态
- 会话为 memory flush turn（仅能追加 memory/2026-05-15.md，无法 exec/交互）
- 微信 Linux 桌面版在前一回合中存在（PID 3940646, 3940720），当前是否还运行未知
- wechat-webapp 目录（/home/w/OCCoding/wechat-webapp）不存在（可能因 compaction 丢失）
- Playwright 和 Chrome 可用（参考 automation/pengzushou/ 中的 login.js）
- 没有 exec/read 沙箱外文件的能力，无法确认当前进程状态

### 待回复要点（下次非 flush 回合时）
1. 需要检查微信桌面版进程是否还在运行
2. 如果不在，要么重启微信桌面版（仍在文件传输模式有问题），要么重启网页版方案
3. 网页版方案：用 Playwright 启动浏览器打开 wx.qq.com，但需要用户扫码登录
4. 桌面版方案：尝试 xdotool 操作（之前无效）或寻找其他自动化方式（DBus、RadiumWMPF IPC）
5. "银杏村"联系人——需要先打开微信聊天列表/搜索找到它

## Turn 3 — 用户选择方案 A

### 用户选择
选择了方案 A：重新启动微信桌面版。

### 限制
当前仍处于 memory flush turn，只能读文件/追加 memory，无法 exec 或写入脚本。用户等待执行。

### 待办
下次非 flush 回合时需要执行 Plan A：
1. 检查 WeChat 桌面进程是否在跑
2. 如果不在，启动 `/usr/bin/wechat`
3. 截图查看状态（可能需要用户扫码登录）
4. 找到"银杏村"对话框并截图

## Turn 4 — 通过 Playwright 打开 wx.qq.com（微信网页版）

### 用户新方向
放弃了桌面版方案（xdotool 无效、调试端口注入复杂），选择让 Playwright 启动 Chrome 打开 https://wx.qq.com。

### 当前状态
- 写入 `wx_web_login.js`（通过 Playwright+Chrome 打开 wx.qq.com 登录页面）
- 屏幕截图显示：Chrome 窗口以 wx.qq.com 打开，显示"使用手机微信扫码登录"
- 用户应已用手机微信扫码登录
- 用户说"Continue the OpenClaw runtime event"——请求继续，即搜索"银杏村"并截图

### 准备中的下一步
- 写 `wx_chat_ginkgo.js` 脚本：连接当前 Chrome，搜索"银杏村"，打开对话框截图
- 但遇到 memory flush 限制（无法写文件/exec）

### 待办（给用户自己动手）
如需手动运行搜索脚本，可执行以下 Node.js 脚本（Playwright + Chrome 连接已有浏览器或打开新会话，搜索"银杏村"并截图到 /tmp/wx_chat_ginkgo.png）

## 2026-05-15

### Session notes

#### wx.qq.com Web WeChat approach (方案C/方案二)
- 用户已扫码登录 wx.qq.com 成功
- 需要使用 Playwright + Chrome 搜索"银杏村"并截图
- 用户当前选择"Continue"，继续网页版方案
- 每次二维码会过期，需用户配合扫码

### Tasks
- [ ] 检查 Chrome 是否还在运行（PID?）
- [ ] 检查 wx.qq.com 页面状态
- [ ] 搜索"银杏村"聊天对话框
- [ ] 截图通过飞书返回用户

Attempt to exec - check Chrome status
