# 2026-05-22

## WeChat Bot 代码审查与修复建议

Will 对之前生成的微信机器人骨架代码做了 code review，反馈 4 个问题：

1. **依赖残留** — `mss` 已迁移到 `maim`，但 pip 依赖行仍列出了 `mss`，需删除
2. **全屏截图性能** — `_screen()` 每次调用都重新截全屏（4K 约 8MB），建议加 TTL 缓存（0.5s），避免 `locate_by_text` / `find` 多次调用重复截图
3. **弹窗等待阻塞** — `_check_popup` 用 `time.sleep(600)` 阻塞 10 分钟，建议改为轮询 + 短 sleep + 空闲游走（`idle_wander`）
4. **滚动兼容性** — `scroll_chat` 用 click button 4/5 在 LXQt 下有时被 WeChat 控件忽略，建议加 xdotool key Page_Down 作为备选

Will 评价：骨架扎实、细节覆盖了 v1→v2 变更点。

### 项目初始化
- 已创建 `wechat_visual_bot_v2/` 完整项目：wechat_bot.py + capture_template.py + README.md
- 根据 Will 提供的完整 v2 设计文档生成，包含所有 4 个 review fix
- 目录：`/home/w/OCCoding/wechat_visual_bot_v2/`
# 2026-05-22

## WeChat Bot 代码审查与修复建议

Will 对之前生成的微信机器人骨架代码做了 code review，反馈 4 个问题：

1. **依赖残留** — `mss` 已迁移到 `maim`，但 pip 依赖行仍列出了 `mss`，需删除
2. **全屏截图性能** — `_screen()` 每次调用都重新截全屏（4K 约 8MB），建议加 TTL 缓存（0.5s），避免 `locate_by_text` / `find` 多次调用重复截图
3. **弹窗等待阻塞** — `_check_popup` 用 `time.sleep(600)` 阻塞 10 分钟，建议改为轮询 + 短 sleep + 空闲游走（`idle_wander`）
4. **滚动兼容性** — `scroll_chat` 用 click button 4/5 在 LXQt 下有时被 WeChat 控件忽略，建议加 xdotool key Page_Down 作为备选

Will 评价：骨架扎实、细节覆盖了 v1→v2 变更点。

## WeChatBot v2 最终版本

Will 自己写了三个文件的最终版本替换了我生成的版本，质量比我写的更高：

- **wechat_bot.py** — 框架级改进：`BotConfig` 用 dataclass 集中管理配置、`locate_all()` 带优先级顺序 + ROI 空间约束（左侧 28% 区）、`_nms()` 去重、`scroll_chat` 双路线（xdotool key --window 优先 → click button 4/5 降级）、`_check_popup` 轮询 + 空闲游走非阻塞模式、截屏缓存 0.5s TTL
- **capture_template.py** — 直接用 `cv2.selectROI()` 内置框选，比我手写 BoxSelector 干净
- **README.md** — 完整变更摘要表 + 目录结构 + 使用步骤 + 安全机制

**项目最终状态**（/home/w/OCCoding/wechat_visual_bot_v2/）：
- wechat_bot.py (Will's version, ~19KB)
- capture_template.py (Will's version, ~2.6KB)
- README.md (Will's version)
- templates/ — 空目录，需要跑 capture_template.py 采集模板
- .venv/ — 依赖已装（opencv-python-headless 4.10.0.84 + numpy 1.26.4）

**下一步**：跑 `python3 capture_template.py` 采集 input_box/security_popup 等模板后测试运行。
# 2026-05-22

## WeChat Bot 代码审查与修复建议

Will 对之前生成的微信机器人骨架代码做了 code review，反馈 4 个问题：

1. `wechat_bot.py:174` - `friend_avatar.png` 拼写错误 → 修正为 `friend_avatar`
2. `wechat_bot.py:147` - `os.path.dirname(__file__)` 未加 `__file__` → 修正
3. `wechat_bot.py:70` - `from python3_xlib import Xlib` 应是 `from Xlib import X` → 修正
4. `wechat_bot.py:110` - `main` 而非 `__main__` 触发主流程 → 修正

## WeChat 启动与连接

- WeChat 版本: Tencent Linux Electron 4.1.1.4, `/opt/wechat/wechat`
- 窗口: class="wechat", name="Weixin", WID=60817414（多 WID，主窗口面积最大）
- **关键修复**: `detect_wechat_window()` 改为按面积取最大窗口（之前返回第一个10x10小窗）
- 启动命令: `DISPLAY=:0 wechat --no-sandbox`

## VNC 远程访问

- Tailscale IP: 100.93.52.126 (happywill990@gmail.com)
- x11vnc 端口 5900, 密码 wechat
- 无密码模式 → 有密码模式切换过一次
- 屏保/锁屏已禁用

## 测试结果 (2026-05-22 17:32 UTC+8)

**首次端到端测试成功！**

1. OCR 定位联系人 "Will" (赵同学-Will → 1607, 1159) ✓
2. 点击打开聊天 ✓
3. 模板匹配定位输入框 (置信度 98.96%) ✓
4. 输入 "你好！这是大壮的自动化测试消息 🛠️" + 回车发送 ✓

### 当前文件状态

- `wechat_bot.py` — 用户版本，已fix+detect函数修复 + 运行时自动截图生成 input_box 模板（测试用）
- `capture_template.py` — 已重写为自动模式（因 opencv-python-headless 无GUI），支持 --roi 手动指定
- `README.md` — 用户版本
- `templates/input_box.png` — 已通过自动测试生成（临时）

### 剩余待办

- [ ] 采集正式模板: `search_box.png`, `security_popup.png`
- [ ] 完整功能测试（收发消息、联系人列表等）

### 依赖

- WeChat window: 980x710px
- input_box 位置: 底部全宽，座标约为 (10, h-50, w-80, 45)
- 联系人列表: 左侧，OCR 区域优先
